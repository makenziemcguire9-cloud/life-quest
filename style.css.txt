* {
  box-sizing: border-box;
}

body {
  margin: 0;
  font-family: Arial, sans-serif;
  background: #f5f5f7;
  color: #222;
}

.top-bar, main {
  max-width: 850px;
  margin: auto;
  padding: 16px;
}

.top-bar {
  display: flex;
  justify-content: space-between;
  align-items: center;
}

.card {
  background: white;
  border-radius: 18px;
  padding: 20px;
  margin-bottom: 16px;
  box-shadow: 0 3px 12px rgba(0,0,0,.08);
}

.hero {
  display: flex;
  justify-content: space-between;
  align-items: center;
}

.eyebrow {
  font-size: 12px;
  font-weight: bold;
  opacity: .6;
  margin-bottom: 4px;
}

.coin-box {
  text-align: center;
  font-size: 18px;
}

.coin-box span {
  display: block;
  font-size: 34px;
}

.progress-bar {
  width: 100%;
  height: 14px;
  background: #e7e7eb;
  border-radius: 999px;
  overflow: hidden;
}

.progress-fill {
  height: 100%;
  width: 0%;
  background: #6c63ff;
  border-radius: 999px;
  transition: width .35s ease;
}

.quest, .room-task, .reward {
  display: flex;
  align-items: center;
  justify-content: space-between;
  gap: 12px;
  padding: 14px 0;
  border-bottom: 1px solid #eee;
}

.quest.completed {
  opacity: .55;
}

.quest button, button {
  border: none;
  border-radius: 10px;
  padding: 10px 14px;
  cursor: pointer;
}

.primary-button, #saveFitness, #saveWeight, #addReward {
  background: #6c63ff;
  color: white;
}

.secondary-button {
  background: #ecebff;
}

.number-row {
  display: flex;
  gap: 8px;
  margin: 10px 0;
}

input {
  min-width: 0;
  flex: 1;
  padding: 11px;
  border: 1px solid #ddd;
  border-radius: 10px;
}

.next-quest {
  font-size: 18px;
  padding: 16px;
  background: #f8f8ff;
  border-radius: 12px;
}

.hidden {
  display: none;
}

footer {
  text-align: center;
  padding: 30px;
  opacity: .65;
}

@media (max-width: 600px) {
  .hero, .top-bar {
    align-items: flex-start;
  }

  .number-row {
    flex-wrap: wrap;
  }

  .number-row input {
    min-width: 120px;
  }
}
